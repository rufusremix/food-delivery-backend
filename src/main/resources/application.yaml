spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_store?createDatabaseIfNotExist=true
    username: root
    password: password

  flyway:
    enabled: true

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.flywaydb: INFO